import { createStore } from 'vuex';

export default createStore({
  state: {
    // 状態をここに追加
  },
  mutations: {
    // 状態を変更するメソッドをここに追加
  },
  actions: {
    // 非同期操作や複雑な状態変更メソッドをここに追加
  },
  getters: {
    // 状態の派生データを取得するメソッドをここに追加
  },
});
